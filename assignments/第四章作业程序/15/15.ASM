DATA SEGMENT
	VAR1 DB 27H
	VAR2 DB 12H
	RESULT DB ?
	REST DB ?
DATA ENDS
CODE SEGMENT
	ASSUME CS:CODE,DS: DATA
START:MOV AX,DATA
	MOV DS,AX
	MOV AX,0H
	MOV BX,0H
	MOV AL,VAR1
	MOV BL,VAR2
	DIV BL
	MOV RESULT,AL
	MOV REST,AH
	JMP $
CODE ENDS
END START 