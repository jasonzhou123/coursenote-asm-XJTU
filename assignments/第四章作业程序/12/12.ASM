DES SEGMENT
	STRING1 DB 'POTATO'
DES ENDS
SOU SEGMENT
	STRING2 DB 'POTATA'
SOU ENDS
CODE SEGMENT
	ASSUME CS:CODE,DS:SOU,ES:DES
START: MOV AX,DES
	MOV ES,AX
	MOV AX,SOU
	MOV DS,AX
	MOV CX,6
	LEA DI,STRING1
	LEA SI,STRING2
	CLD
	REPZ CMPSB 
	JNZ LAB
	MOV AL,1
	JMP DONE
LAB:  MOV AL,0
DONE: JMP $
CODE ENDS
END START 