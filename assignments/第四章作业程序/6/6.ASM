DATA SEGMENT
	XL DW 1278H 
	XH DW 8221H
	Y DW 3A62H
	Z DB 6 DUP(?)
DATA ENDS 
CODE SEGMENT
	ASSUME CS:CODE,DS:DATA
START: MOV AX,DATA
	MOV DS,AX
	MOV CX,Y
	MOV AX,XL
	LEA SI,Z
	MUL CX
	MOV [SI],AX
	MOV BX,DX
	MOV AX,XH
	MUL CX
	ADD AX,BX
	MOV [SI+2],AX
	MOV [SI+4],DX
CODE ENDS
END START
