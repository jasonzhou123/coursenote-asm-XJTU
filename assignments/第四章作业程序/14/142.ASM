DATA SEGMENT
	N DB 1H
DATA ENDS
CODE SEGMENT
	ASSUME CS:CODE,DS:DATA
START: 
	MOV AX,DATA
	MOV DS,AX
	MOV AH, 25H
	MOV AL, 42H ;设置中断号
	MOV DX, SEG myInt; myInt是中断处理程序名称
	MOV DS, DX
	MOV DX, OFFSET myInt
	INT 21H
	JMP $

myInt PROC FAR
	MOV AX,0H
	IRET
myInt	ENDP

CODE ENDS
END START 